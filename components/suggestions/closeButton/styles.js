import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({

  container: {
    right: 0,
    padding: 0,
    color: 'red',
    zIndex: 50,
    position: 'absolute',
  },

});

export default styles;
