import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({

  container: {
    top: 10,
    right: 13,
    color: 'red',
    zIndex: 50,
    position: 'absolute',
  },

});

export default styles;
