import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({

  container: {
    top:130,
    left:240,
    position: 'absolute',

  },
  currentLocation: {
    width: 25,
    resizeMode: 'contain',
  }

});

export default styles;