import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({

  container: {
    top:78,
    left:33,
    position: 'absolute',

  },
  currentLocation: {
    width: 10,
    resizeMode: 'contain',
  }

});

export default styles;