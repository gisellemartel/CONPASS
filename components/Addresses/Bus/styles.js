import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({

  container: {
    top: 130,
    left: 105,
    position: 'absolute',
    zIndex: 2,
  },
  currentLocation: {
    width: 15,
    resizeMode: 'contain',
  }

});

export default styles;
