import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({

  container: {
    top:20,
    left:30,
    position: 'absolute',

  },
  currentLocation: {
    width: 17,
    resizeMode: 'contain',
  }

});

export default styles;