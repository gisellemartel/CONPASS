import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({

  container: {
    top: 130,
    left: 220,
    position: 'absolute',
    zIndex: 201,
  },
  currentLocation: {
    width: 15,
    resizeMode: 'contain',
  }

});

export default styles;
