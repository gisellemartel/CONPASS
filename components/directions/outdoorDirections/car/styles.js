import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({

  container: {
    top: 130,
    left: 30,
    zIndex: 201,
    position: 'absolute',
  },
  currentLocation: {
    width: 15,
    resizeMode: 'contain',
  }

});

export default styles;
