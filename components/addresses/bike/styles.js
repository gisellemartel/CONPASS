import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({

  container: {
    top: 130,
    left: 130,
    zIndex: 201,
  },
  currentLocation: {
    width: 25,
    resizeMode: 'contain',
    zIndex: 201,
  }

});

export default styles;
