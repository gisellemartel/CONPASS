import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({

  container: {
    top: 140,
    left: 130,
    zIndex: 2,
  },
  currentLocation: {
    width: 25,
    resizeMode: 'contain',
  }

});

export default styles;
