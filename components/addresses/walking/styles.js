import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({

  container: {
    top: 145,
    left: 216,
    position: 'absolute',
    zIndex: 2,


  },
  currentLocation: {
    width: 15,
    resizeMode: 'contain',
  }

});

export default styles;
