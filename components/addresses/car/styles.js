import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({

  container: {
    top: 146,
    left: 30,
    zIndex: 2,
    position: 'absolute',
  },
  currentLocation: {
    width: 15,
    resizeMode: 'contain',
  }

});

export default styles;
