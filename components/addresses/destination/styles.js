import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    left: 60,
    position: 'absolute',
    top: 70
  },
  currentLocation: {
    width: 10,
    resizeMode: 'contain',
  }
});

export default styles;
