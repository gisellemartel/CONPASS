import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({

  container: {
    left: 60,
    position: 'absolute',
    zIndex: 201,
    top: 70
  },
  currentLocation: {
    width: 10,
    resizeMode: 'contain',
    zIndex: 201,
  }

});

export default styles;
