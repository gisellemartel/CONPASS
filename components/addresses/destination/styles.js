import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({

  container: {
    left: 61,
    position: 'absolute',

    zIndex: 2,
    top: 90
  },
  currentLocation: {
    width: 10,
    resizeMode: 'contain',
  }

});

export default styles;
