import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({

  container: {
    left: 58,
    top: 10,
    position: 'absolute',
    zIndex: 201,
  },
  currentLocation: {
    width: 19,
    resizeMode: 'contain',
    zIndex: 201,
  }

});

export default styles;
