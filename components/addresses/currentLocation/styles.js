import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({

  container: {
    left: 56,
    top: 26,
    position: 'absolute',
    zIndex: 2,
  },
  currentLocation: {
    width: 19,
    resizeMode: 'contain',
  }

});

export default styles;
