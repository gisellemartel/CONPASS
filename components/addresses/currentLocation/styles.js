import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    left: 40,
    top: 10,
    position: 'absolute',
  },
  currentLocation: {
    width: 19,
    resizeMode: 'contain',
  }
});

export default styles;
