import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({

  container: {
    top: 15,
    left: 30,
    position: 'absolute',

  },
  currentLocation: {
    width: 19,
    resizeMode: 'contain',
  }

});

export default styles;
